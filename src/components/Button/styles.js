import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
   container: {
    backgroundColor: "blue",
    paddingVertical: 20,
    paddingHorizontal: 8,
    borderRadius: 10,
    width: "100%"

   },
   title: {
    color: "white",
    fontSize: 16,
    fontWeight: "bold",
    textAlign: 'center'
},
});